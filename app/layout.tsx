import type { Metadata } from "next";
import { Outfit, Inter } from "next/font/google";
import "./globals.css";

const outfit = Outfit({
  variable: "--font-outfit",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ourika Travels | Authentic local experiences in Ourika Valley",
  description: "Discover the hidden gems of Ourika Valley with expert local guides. Book unique hikes, cultural tours, and authentic Moroccan experiences.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${outfit.variable} ${inter.variable} font-outfit antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
